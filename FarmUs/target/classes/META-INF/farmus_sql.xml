<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.farmus.service.dao.farmusDAO">

<select id="loginMember" resultType="memberVO">
		SELECT m_name, m_point, a_num fROM member
		WHERE m_mail = #{m_mail}
		and m_pass = #{m_pass} 
	</select>
	
<select id="selectBoard" resultType="boardVO" parameterType="String">
    select b_title, m_name, b_date from board
</select>
    
</mapper>